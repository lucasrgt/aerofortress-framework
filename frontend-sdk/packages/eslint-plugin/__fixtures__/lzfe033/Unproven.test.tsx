// Fixture for AFFE033 (verify-has-avp-proof): this proof is for a DIFFERENT criterion, so a co-located
// Unproven.view.tsx whose obligation is "needs-y" stays unproven (the test exists but proves only other-z).
/** @avp other-z */
export {};
