<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="..\..\build\Lazuli.Library.props" />

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
    <IsRoslynComponent>true</IsRoslynComponent>

    <!-- Ship as a NuGet analyzer: the DLL goes under analyzers/dotnet/cs (not lib/), carries no
         runtime dependencies, and does not flow as a transitive reference to consumers. -->
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <DevelopmentDependency>true</DevelopmentDependency>
    <!-- An analyzer ships no lib assembly, so there's nothing to put in a symbol package — don't emit an empty one. -->
    <IncludeSymbols>false</IncludeSymbols>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Lazuli.SelfHarness\Lazuli.SelfHarness.csproj"
                      OutputItemType="Analyzer" ReferenceOutputAssembly="false" PrivateAssets="all" />
  </ItemGroup>

  <!-- Ship the analyzer so it flows TRANSITIVELY (e.g. via the Lazuli meta-package): the DLL + a buildTransitive
       target that adds it to @(Analyzer) for every consumer, direct and transitive. The DLL goes in
       buildTransitive/ (not analyzers/) so NuGet does not ALSO auto-apply it to direct refs — the target is the
       single wiring, no double-add. See buildTransitive/Lazuli.Doctor.targets. -->
  <ItemGroup>
    <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="buildTransitive/Lazuli.Doctor.dll" Visible="false" />
    <None Include="buildTransitive/Lazuli.Doctor.targets" Pack="true" PackagePath="buildTransitive/Lazuli.Doctor.targets" Visible="false" />
  </ItemGroup>

</Project>
