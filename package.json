{
  "name": "lazuli-net",
  "private": true,
  "description": "Repo-level dev tooling (git hooks) for lazuli-net. The framework itself is .NET (Lazuli.slnx); the frontend lives in the frontend-sdk workspace. This manifest exists only so lefthook can manage the hooks.",
  "scripts": {
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.10.10"
  }
}
