// The kit's single import seam to the app's tokens. If your tokens live elsewhere, repoint THIS file —
// every primitive imports from here, never from the tokens path directly.
export * from "@/design/tokens";
